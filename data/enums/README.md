# Enums

This directory stores public enum definitions used by the archive data.

The initial enum files define record statuses, entity types, categories, event types, source types, reliability labels, and confidence labels.
