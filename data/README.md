# Data Directory

This directory will contain public, source-linked archive data.

Planned structure:

```text
data/
  records/
  enums/
```

PR-001 does not add canonical records yet. Seed records should be added in a later PR after schema and validation rules are defined.

Data files should contain only public archive data, public sources, and public metadata used by the site.
