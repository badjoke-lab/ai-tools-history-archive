# Records

This directory is reserved for future per-record files.

PR-002 starts with `data/records.ts` so the initial dataset can be imported directly by the static site. Future PRs may split records into one file per slug if that becomes easier to review.

All record data must be public, source-linked, neutral, and safe to publish.
